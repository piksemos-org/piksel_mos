import 'package:flutter/material.dart';

class MenungguPembayaranScreen extends StatelessWidget {
  const MenungguPembayaranScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Menunggu Pembayaran')),
      body: const Center(child: Text('Halaman Menunggu Pembayaran')),
    );
  }
}