// lib/screens/profile/navigasi/chat_piksel_mos_screen.dart
import 'package:flutter/material.dart';

class ChatPikselMosScreen extends StatelessWidget {
  const ChatPikselMosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Chat Piksel.mos'),
        backgroundColor: Colors.deepPurple,
        foregroundColor: Colors.white,
      ),
      body: const Center(
        child: Text('Chat Piksel.mos Screen (Placeholder)'),
      ),
    );
  }
}